server:
  port: 8769
eureka:
  client:
    service-url:
      defaultZone:
        http://registry-server-2:8762/eureka
feign:
  hystrix:
    enabled: true
logging:
  file: "logs/service-consumer-feign.log"
management:
  security:
    enabled: false
